language: dart
dart:
  - stable
cache:
  directories:
    - $HOME/.pub-cache
script:
  - bash ./deploy.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build/web
  on:
    branch: master
